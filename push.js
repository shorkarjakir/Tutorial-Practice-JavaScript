// use puse to add Element in the last Element
var numbers = [20, 25, 30];
numbers.push(35);
// console.log(numbers);

// 2. use pop to reduce last one 
numbers.pop();
numbers.pop();
console.log(numbers);
